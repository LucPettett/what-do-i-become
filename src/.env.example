# what-do-i-become configuration
# Copy to .env and fill in your values: cp framework/.env.example framework/.env

# Provider selection
WDIB_LLM_PROVIDER=openai
# Runtime support today: openai
# Planned providers: anthropic, grok/xai, gemini

# Model to use
WDIB_LLM_MODEL=gpt-5

# Provider API keys (set the one matching WDIB_LLM_PROVIDER)
OPENAI_API_KEY=sk-your-key-here
ANTHROPIC_API_KEY=your-anthropic-key-here
XAI_API_KEY=your-xai-key-here
GEMINI_API_KEY=your-gemini-key-here
GOOGLE_API_KEY=your-google-ai-key-here

# Max tool-call iterations per session (default: 25)
WDIB_MAX_ITERATIONS=25

# Command execution timeout in seconds (default: 300)
WDIB_CMD_TIMEOUT=300

# Device identity (generated by framework/setup.sh)
WDIB_DEVICE_ID=

# Git behavior for auto-commits/pushes
WDIB_GIT_REMOTE=origin
WDIB_GIT_BRANCH=
WDIB_GIT_AUTO_PUSH=true

# Optional local git identity override
WDIB_GIT_USER_NAME=
WDIB_GIT_USER_EMAIL=

# Optional: setup can add remote if missing
WDIB_GIT_REMOTE_URL=
