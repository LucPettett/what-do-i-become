# what-do-i-become configuration
# Copy to src/.env and fill in your values:
#   cp src/.env.example src/.env

# LLM provider and model
WDIB_LLM_PROVIDER=openai
WDIB_LLM_MODEL=gpt-5
WDIB_CODEX_MODEL=

# Optional model aliases (backward compatibility)
WDIB_MODEL=
PI_AGENT_MODEL=

# Provider API keys (set the one matching WDIB_LLM_PROVIDER)
# For codex exec in unattended runs, CODEX_API_KEY is preferred; if unset WDIB falls back to OPENAI_API_KEY.
OPENAI_API_KEY=sk-your-key-here
CODEX_API_KEY=
ANTHROPIC_API_KEY=your-anthropic-key-here
XAI_API_KEY=your-xai-key-here
GEMINI_API_KEY=your-gemini-key-here
GOOGLE_API_KEY=your-google-ai-key-here

# Runtime guardrails
WDIB_MAX_ITERATIONS=25
WDIB_CMD_TIMEOUT=300

# Optional legacy aliases (backward compatibility)
PI_AGENT_MAX_ITERATIONS=
PI_AGENT_CMD_TIMEOUT=

# Device identity (auto-generated by src/setup.sh)
WDIB_DEVICE_ID=

# Git behavior for auto-commits/pushes
WDIB_GIT_REMOTE=origin
WDIB_GIT_BRANCH=
WDIB_GIT_AUTO_PUSH=true

# Optional local git identity override
WDIB_GIT_USER_NAME=
WDIB_GIT_USER_EMAIL=

# Optional: set your target repo URL (setup will add/update WDIB_GIT_REMOTE)
WDIB_GIT_REMOTE_URL=

# Notifications (modular integrations)
# Comma-separated channels; currently supported: slack
WDIB_NOTIFICATION_CHANNELS=

# Slack incoming webhook integration (optional)
# Keep this secret and set it only in src/.env on your device.
WDIB_SLACK_WEBHOOK_URL=
WDIB_SLACK_USERNAME=wdib
# Optional legacy single-icon override (used when specific emojis are unset)
WDIB_SLACK_ICON_EMOJI=
# Defaults: awakening day uses :sunrise:, normal updates use ☕️
WDIB_SLACK_AWAKENING_EMOJI=:sunrise:
WDIB_SLACK_UPDATE_EMOJI=☕️
WDIB_SLACK_TIMEOUT_SECONDS=8
# Slack message style: human (default) or detailed
WDIB_SLACK_MESSAGE_STYLE=human
